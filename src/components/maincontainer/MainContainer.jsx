import React from "react";
import VideoContainer from "./VideoContainer";

const MainContainer = () => {
  return (
    <div className="mx-8">
      <VideoContainer />
    </div>
  );
};

export default MainContainer;
